<?php
 
/**
* @file
* Shanti Sarvaka header region template overrides default to remove <div> wrapper
* Each block in header is turned into a bootstrap nav dropdown menu.
*/
 
if ($content): ?>
    <?php print $content; ?>
<?php endif; ?>